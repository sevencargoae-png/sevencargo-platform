# دليل استرجاع منصة SEVENCARGO

الدليل ده بيشرح إزاي ترجّع الموقع من النسخة الاحتياطية لو حصل أي حاجة: الحساب اتقفل، أو القاعدة اتمسحت، أو عاوز تنقل على سيرفر تاني.

## النسخة الاحتياطية فيها إيه
1. **الكود:** ملف `sevencargo-code-*.zip`. نفس الكود اللي على GitHub.
2. **البيانات:** ملف `sevencargo-backup-*.json`. فيه الطلبات والمندوبين والحضور والصور والرسائل والشكاوى والتقييمات والإعدادات والأسعار وحسابات الموظفين.
   - بتنزّله بنفسك من **لوحة الإدارة ← حسابي ← تنزيل النسخة الاحتياطية**.
   - **نزّل نسخة جديدة كل أسبوع** على الأقل، واحفظها في مكان آمن زي Google Drive. الملف فيه بيانات العملاء، فما تبعتوش لحد.

## الحماية اللي موجودة على Render
- **Point-in-Time Recovery:** Render بيحتفظ بنسخة مستمرة من القاعدة المدفوعة لآخر 3 أيام، وبتقدر ترجّعها لأي لحظة في الفترة دي. الطريقة: Dashboard ← sevencargo-db ← Recovery.
- **Export:** من نفس صفحة Recovery تقدر تعمل Export وتنزّله، والملف بيفضل موجود على Render لمدة 7 أيام.
- النسخة الأسبوعية اللي بتنزّلها بنفسك هي اللي بتحميك من أي حاجة أقدم من كده.

## الاسترجاع الكامل (سيرفر جديد)
1. **الكود:** ارفعه على مستودع GitHub جديد، أو استخدم المستودع الحالي.
2. **على Render:** اعمل New ← Blueprint واختار المستودع. ملف `render.yaml` هيعمل قاعدة البيانات وخدمة الموقع ويربطهم ببعض لوحده.
3. **متغيرات البيئة** في خدمة الموقع:
   - `NODE_ENV=production`
   - `JWT_SECRET`: أي نص طويل عشوائي.
   - `ADMIN_USERNAME` و `ADMIN_PASSWORD`
   - `PUBLIC_URL=https://sevencargo.online`
   - `CANONICAL_HOST=sevencargo.online`
   - `NODE_VERSION=22`
4. **البيانات:** بعد أول Deploy ناجح، افتح Shell خدمة الموقع على Render وشغّل:
   ```
   node scripts/restore.js sevencargo-backup-XXXX.json
   ```
   - الملف لازم يكون جوه السيرفر الأول: ارفعه على المستودع مؤقتًا، أو اطلب من Claude يعملها.
   - السكربت **بيمسح البيانات الموجودة** في القاعدة الجديدة وبيحط بدالها بيانات النسخة.
   - لو حصل أي خطأ في النص، مفيش أي حاجة بتتغير.
5. **الدومين:** Settings ← Custom Domains ← ضيف `sevencargo.online` و `www.sevencargo.online`، وبعدين في Hostinger:
   - A `@` → `216.24.57.1`
   - CNAME `www` → عنوان onrender.com الجديد
   - من غير سجل AAAA.
6. **المراقبة:** في UptimeRobot حدّث الرابط لو اتغيّر، وفي Render خلّي Health Check Path على `/healthz`.

## ملاحظات
- كلمات مرور الموظفين والمندوبين بترجع زي ما كانت، لأنها متخزنة مشفّرة جوه النسخة.
- لو استرجعت على قاعدة فاضية، الجداول بتتعمل تلقائي قبل الاسترجاع.
